# P2P Port Forwarding System (P2PForFriend)

ระบบ P2P Tunneling อย่างง่ายที่ช่วยให้คุณสามารถเปิด Local Port (เช่น Minecraft Server, Web Server) ออกสู่สาธารณะผ่านเครื่อง Server กลางได้ โดยไม่ต้องตั้งค่า Forward Port ที่ Router ฝั่ง Client

## 📁 โครงสร้างไฟล์

* **`serverp2p.py`**: โปรแกรมฝั่ง Server ทำหน้าที่จัดการ Port และเป็นตัวกลางส่งข้อมูล (Tunnel)
* **`p2p_gui.py`**: โปรแกรมฝั่ง Client แบบมีหน้าต่างกราฟิก (GUI) สำหรับผู้ใช้งานทั่วไป
* **`clientp2p.py`**: โปรแกรมฝั่ง Client แบบ Command Line (CLI) สำหรับผู้ใช้ขั้นสูงหรือรันบน Server
* **`p2p_gui.spec`**: ไฟล์ตั้งค่าสำหรับ PyInstaller (ใช้กรณีต้องการ Build เป็น .exe)

## 📋 สิ่งที่ต้องเตรียม (Prerequisites)

* **Python 3.x** (ติดตั้งได้จาก [python.org](https://www.python.org/))
* เครื่อง Server ที่มี **Public IP** (สำหรับรัน `serverp2p.py`)

## 🚀 วิธีการใช้งาน

### 1. ฝั่ง Server (เครื่องที่มี Public IP)
รันสคริปต์ Server เพื่อเปิดรอรับการเชื่อมต่อ
```bash
python serverp2p.py
```
### 2. ฝั่ง client 
รูปแบบ: python clientp2p.py <SERVER_IP> <CONTROL_PORT> <LOCAL_PORT>
```bash
python clientp2p.py xxx.xxx.xxx.xxx 9000 25565
```
