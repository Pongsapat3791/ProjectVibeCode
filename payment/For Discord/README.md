# 🎁 TrueMoney Wallet Gift Discord Bot

บอท Discord สำหรับรับลิงก์ซองของขวัญ TrueMoney Wallet ผ่านระบบ Modal (ฟอร์มกรอก) และทำการเติมเข้าเบอร์โทรศัพท์ที่กำหนดอัตโนมัติ

## ✨ ความสามารถ
- มีปุ่มกดเพื่อให้ User กรอกลิงก์ซองของขวัญ
- ตรวจสอบความถูกต้องของลิงก์ (Validator)
- เติมเงินเข้าเบอร์ Wallet อัตโนมัติทันทีที่กรอก
- แจ้งเตือนเมื่อเติมสำเร็จไปยังห้อง Log ที่กำหนด

## 🛠️ การติดตั้ง (Installation)

1. **Clone หรือดาวน์โหลดโปรเจกต์นี้**
2. **ติดตั้ง Library ที่จำเป็น**
   เปิด Terminal หรือ Command Prompt แล้วพิมพ์คำสั่ง:
   ```bash
   pip install -r requirements.txt
   ```
### 📝 สิ่งที่คุณต้องทำเพิ่มเติม (สำคัญมาก):

จากโค้ดต้นฉบับ มีจุดที่ต้องระวังครับ:

1.  **สร้างไฟล์ `settingall.py`**: โค้ดบรรทัดที่ `7` และ `155` เรียกใช้ Class นี้ ถ้าไม่มีไฟล์นี้โปรแกรมจะ Error 
2.  **เปลี่ยนเบอร์โทรศัพท์**: ในบรรทัดที่ `25` (`self.receiver_phone_number = "0123456789"`) ตอนนี้เป็นเบอร์สมมติ ถ้าไม่แก้ เงินจะไม่เข้ากระเป๋าคุณครับ
3.  **เปลี่ยน Channel ID**: ในบรรทัดที่ `127` (`await bot.get_channel(12345678901234567)...`) ต้องเปลี่ยนเลขเป็น ID ห้องในเซิร์ฟเวอร์ของคุณเองครับ